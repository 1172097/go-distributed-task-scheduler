global:
  scrape_interval: 2s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: api
    static_configs:
      - targets: ["host.docker.internal:2112"]
  - job_name: worker
    static_configs:
      - targets: ["host.docker.internal:2113"]
